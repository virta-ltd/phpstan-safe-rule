{
	"name": "PHPStan-Safe-Rule",
	"build": {
		"context": "..",
		"dockerfile": "./Dockerfile"
	},

	"workspaceMount": "source=${localWorkspaceFolder},target=/psr,type=bind",
	"workspaceFolder": "/psr"
}
